const AddRandomContact = () => {
  return (
    <div>
      <button className="btn btn-danger form-control">
        Add random contact
      </button>
    </div>
  );
};

export default AddRandomContact;
